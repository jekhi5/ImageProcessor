package gui.swingview;

/**
 * Represents a bar where the user can scroll through the loaded images and select the one they
 * would like to work on.
 */
public class ImageViewerBar implements ImageEditorSwingFeature {

  
}
